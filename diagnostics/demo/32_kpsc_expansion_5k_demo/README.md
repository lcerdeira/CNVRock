# Demo: exp 32 (5K) — 200-sample subsample

First 200 samples (alphabetical) from `data/results/32_kpsc_expansion_5k/`.

This subset exists ONLY so the Streamlit diagnostics app on Streamlit
Community Cloud has something to render — the full results directory is
gitignored because of `reconstructions.npy` size (~100 MB for 5K samples).

Generated by `make_demo.py` from commit at time of build.
